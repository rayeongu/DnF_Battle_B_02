sequenceDiagram
    autonumber
    participant UI as <<boundary>><br/>Attack_Monster_UI
    participant Battle as 전투
    participant P as 플레이어(Player)
    participant Char as 캐릭터(Character)

    UI->>Battle: 몬스터공격(플레이어id, character)
    activate Battle

    Battle->>P: 플레이어체크(플레이어id)
    activate P
    P-->>Battle: return true (인증 성공)
    deactivate P

    alt character != null
        # 1. 스킬발동 호출 (데미지 계산)
        Battle->>Char: 스킬발동()
        activate Char
        alt 직업 == "전사"
            Char-->>Battle: return "검 휘두르기!" return (this.공격력 * 1.5)
        else 직업 == "마법사"
            Char-->>Battle: return "파이어볼!" return (this.공격력 * 2.0)
        end
        deactivate Char



        # 3. 데미지에 따른 등급 판정 (전투 클래스 내부 로직)
        alt 데미지 >= 200
            Battle->>Battle: 등급 = "S급 공격"
        else 데미지 >= 100
            Battle->>Battle: 등급 = "A급 공격"
        else 데미지 < 100
            Battle->>Battle: 등급 = "B급 공격"
        end

        Battle-->>UI: 최종 결과 문자열 반환
    else character == null
        Battle-->>UI: return "생성된 캐릭터가 없습니다."
    end

    deactivate Battle