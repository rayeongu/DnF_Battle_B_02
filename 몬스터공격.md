```mermaid
sequenceDiagram
    autonumber
    participant UI as <<boundary>><br/>Attack_Monster_UI
    participant Battle as 전투
    participant P as 플레이어(Player)
    participant Char as 캐릭터(Character)

    UI->>Battle: 몬스터 공격 요청(id)
    activate Battle

    Battle->>P: 플레이어체크(id)
    activate P
    P-->>Battle: 체크 결과 (boolean)
    deactivate P

    alt id == "hero"
        Battle->>Char: 스킬발동()
        activate Char
        
        alt 직업 == "전사"
            Char->>Char: return "검 휘두루기!" <br> this.데미지 = 공격력 * 1.5
        else 직업 == "마법사"
            Char->>Char: return "파이어볼" <br> this.데미지 = 공격력 * 2.0
        end
        
        Char-->>Battle: 스킬명 및 데미지 반환
        deactivate Char

        alt 데미지 >= 200
            Battle->>Battle: this.등급 = "S급"
        else 데미지 >= 100
            Battle->>Battle: this.등급 = "A급"
        else 데미지 < 100
            Battle->>Battle: this.등급 = "B급"
        end
        
        Battle-->>UI: 최종 결과(스킬, 데미지, 등급) 전송
    else id != "hero"
        Battle-->>UI: 공격 실패 메시지 전송
    end

    deactivate Battle