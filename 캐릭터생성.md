```mermaid
sequenceDiagram
    autonumber
    participant UI as <<boundary>><br/>Create_Character_UI
    participant Battle as 전투
    participant P as 플레이어(Player)
    participant Char as <<abstract>><br/>캐릭터(Character)

    UI->>Battle: 캐릭터생성(플레이어id, 캐릭터명, 직업, 레벨)
    activate Battle
    
    # 1. 플레이어 체크 호출
    Battle->>P: 플레이어체크(플레이어id)
    activate P
    P-->>Battle: return true (id=="hero")
    deactivate P

    alt 플레이어체크 성공 (true)
        alt 직업 == "전사"
            # 2. 전사 객체 생성 (생성자 호출)
            Battle->>Char: new 전사(캐릭터명, 레벨)
            activate Char
            Char->>Char: this.HP = 레벨 * 100
            Char->>Char: this.공격력 = 레벨 * 15
            Char-->>Battle: return 전사 인스턴스
            deactivate Char
        else 직업 == "마법사"
            # 2. 마법사 객체 생성 (생성자 호출)
            Battle->>Char: new 마법사(캐릭터명, 레벨)
            activate Char
            Char->>Char: this.HP = 레벨 * 60
            Char->>Char: this.공격력 = 레벨 * 25
            Char-->>Battle: return 마법사 인스턴스
            deactivate Char
        end
        Battle-->>UI: 생성된 캐릭터 객체 반환
    else 플레이어체크 실패 (false)
        Battle-->>UI: return null
    end
    
    deactivate Battle
